import type { PropsWithChildren } from "react";

export const Header = (props: PropsWithChildren) => {
  return (
    <header className="flex border-b bg-zinc-900 border-zinc-700 overflow-none p-4">
      <div className="flex flex-col w-full">
        bcleaks
      </div>
    </header>
  );
};
